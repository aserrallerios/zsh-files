# Global Aliases
alias -g clip='| xclip -selection clipboard'
alias -g G=' | grep'
alias -g GI=' | grep -i  '
alias -g L=' | less'
alias -g T=' | tail -20'
alias -g NUL=' > /dev/null 2>&1'
alias -g N=' | /dev/null'
alias -g C=' | wc -l'
alias -g H=' | head'
alias -g S=' | sort'

# Extensions
alias -s html='firefox'
alias -s log='less -MN'
alias -s md=$EDITOR
alias -s txt=$EDITOR
